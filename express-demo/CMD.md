# Initialize npm
In directory,
$ npm init --yes

# Install express
$ npm i express

# Install nodemon (automatically restart when changing the codes)
$ npm i -g nodemon

# Install joi (Valdation)
$ npm i joi